mod test;

/*
 * TODO for runtime:
 * - (Later Milestone) Multithreading ends on a blank cmd.
 */

//TODO Move this to runtime!
/*pub struct RegisterStorage<'a> {
    reg1: &'a (Types, Box<dyn Any>),
    reg2: &'a (Types, Box<dyn Any>),
    reg_return: &'a (Types, Box<dyn Any>)
}*/
